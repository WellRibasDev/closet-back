export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 24 }}>
      <h1>Closet API</h1>
      <p>
        Backend REST em <code>/api</code>. Veja <code>GET /api/health</code>.
      </p>
    </main>
  );
}
